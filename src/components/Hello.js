import React from 'react';

function Hello()
{
    return(
        <h1>
            Hello, World!
        </h1>
    );
}

// If we want to import our funciton to other files then we have to export here..!
export default Hello;
